# Blog Website
- Run it using ```$ node app.js```
- Has 4 main routes:
  - ```'/'``` home route
  - ```'/compose'``` route
  - ```'/about'``` route
  - ```'/contact'``` route
- To install the required packages run ```$ npm install```
