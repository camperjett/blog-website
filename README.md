# Blog Website
- To install the required packages (dependencies) run ```$ npm install```
- Run it using ```$ node app.js```
- Has 4 main routes:
  - ```'/'``` home route
  - ```'/compose'``` route
  - ```'/about'``` route
  - ```'/contact'``` route

